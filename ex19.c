/*#include <stdio.h>

int main() {
    float moy;
    int filiere;

    printf("Moyenne : ");
    scanf("%f", &moy);

    printf("Filiere (1-Info, 2-Reseau, 3-Gestion) : ");
    scanf("%d", &filiere);

    // Vérification avec ternaire
    printf("Resulta %s",((filiere=1 && moy>=14)||(filiere=2 && moy>=12)||(filiere = 3 && moy>=10))?"Accepte":"refuse");


    return 0;
}
*/
