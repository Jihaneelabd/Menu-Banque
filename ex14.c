/*#include <stdio.h>

int main() {
    float moyenne;

    printf("Entrez la moyenne : ");

    scanf("%f", &moyenne);

    // Classification avec ternaires imbriqués
   printf(moyenne >= 16 ? "Excellent" : moyenne >= 14 ? "Tres Bien" : moyenne >= 12 ? "Bien" : moyenne >= 10 ? "Passable" : "Echec");

    return 0;
}
*/
