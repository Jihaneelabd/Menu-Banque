/*#include <stdio.h>

int main() {
    int Q;
    float P, brut, remise, net;

    printf("Entrez la quantite : ");
    scanf("%d",&Q);

    printf("Entrez le prix unitaire : ");
    scanf("%f",&P);

     brut = Q*P;

    remise = (Q >= 50) ? brut * 0.2 : (Q >= 20) ? brut * 0.1 : 0;

    net = brut* remise;

    // Affichage des résultats
    printf("Brut : %.2f DH\n", brut);
    printf("Remise : %.2f DH\n", remise);
    printf("Net : %.2f DH\n", net);

    return 0;
}
*/
