/*#include <stdio.h>

int main() {
    float salaire, total;
    int annee;

    printf("Salaire de base : ");
    scanf("%f", &salaire);

    printf("Anciennete (ans) : ");
    scanf("%d", &annee);

    // Calcul du salaire total avec ternaire
    total = (annee>=10)? salaire*1.15:(annee>=5)?salaire*1.1:(annee>=2)?salaire*1.05:salaire;

    // Affichage du salaire total
    printf("Salaire total : %.2f DH", total);

    return 0;
}
*/
