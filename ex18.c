/*#include <stdio.h>

int main() {
    float go;
    int prix;

    printf("Consommation (Go) : ");
    scanf("%f", &go);

    // Calcul du prix avec ternaires imbriqués
    prix = (go<=5)? 20:(go<=10)?35:(go<=20)?50:80;

    // Affichage du prix
    printf("Prix : %d DH", prix);

    return 0;
}
*/
