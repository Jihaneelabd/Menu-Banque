/*#include <stdio.h>

int main() {
    int H;
    int prix;

    printf("Entrez le nombre d'heures : ");
    scanf("%d", &H);

    // Calcul du prix avec ternaire
    prix =(H<=1)?5:(5 + (H - 1) * 3);

    // Appliquer le plafond
    prix= (H>30)?30:prix;

    // Affichage du prix
    printf("Prix a payer : %d DH", prix);

    return 0;
}
*/
