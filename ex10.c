/*#include <stdio.h>

int main(){
    float prix, prixFinal;
    printf("Entrez le prix : ");
    scanf("%f",&prix);
    prixFinal=prix;

    prixFinal = (prix >= 500) ? prix * 0.9 : prix;
    printf("%f",prixFinal);

    return 0;
}
*/
