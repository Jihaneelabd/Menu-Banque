/*#include <stdio.h>
#include <stdlib.h>

int main() {
    int a, b, resultat;

    printf("Entrez le premier nombre : ");
    scanf("%d",&a);

    printf("Entrez le deuxieme nombre : ");
    scanf("%d",&b);

   resultat = (abs(a) < abs(b)) ? a : b;


    printf("Le nombre le plus proche de zero est %d",resultat);

    return 0;
}
*/
